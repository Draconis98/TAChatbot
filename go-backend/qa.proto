syntax = "proto3";

option go_package = "/api";

import "google/protobuf/empty.proto";

// 定义QA消息结构
message QA {
  string question = 1;
  string answer = 2;
}

// 定义QA服务
service QAServer {
  rpc PushQA(QA) returns (google.protobuf.Empty) {}
}